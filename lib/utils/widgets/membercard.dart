import 'package:flutter/material.dart';

class MemebersCard extends StatefulWidget {
  MemebersCard({Key? key}) : super(key: key);

  @override
  _MemebersCardState createState() => _MemebersCardState();
}

class _MemebersCardState extends State<MemebersCard> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
